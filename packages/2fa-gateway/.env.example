# 2FA Gateway Configuration

# ── Internal 2FA Server ────────────────────────────────────────────────
# URL of the 2fa-telegram server (internal network)
TFA_INTERNAL_URL=http://localhost:3200

# ── Telegram Bot Token ─────────────────────────────────────────────────
# Same token as 2fa-telegram. If set, the webhook is auto-registered on startup.
TELEGRAM_BOT_TOKEN=123456789:ABC-DEF1234ghIkl-zyx57W2v1u123ew11

# ── Webhook URL (Optional) ─────────────────────────────────────────────
# Public URL for Telegram webhook. Auto-detected on Railway via RAILWAY_PUBLIC_DOMAIN.
# Only set this manually for local development (e.g., ngrok URL).
# WEBHOOK_URL=https://your-ngrok-url.ngrok.io/webhook

# ── Gateway Port ───────────────────────────────────────────────────────
# This port must be publicly accessible for Telegram webhooks
TFA_GATEWAY_PORT=3201
