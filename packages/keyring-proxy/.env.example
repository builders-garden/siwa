# Keyring Proxy Configuration

# ── Required ───────────────────────────────────────────────────────────
# Shared HMAC secret for signing operations
# Generate with: openssl rand -hex 32
KEYRING_PROXY_SECRET=your-secure-random-secret-here

# ── Server Configuration ───────────────────────────────────────────────
KEYRING_PROXY_PORT=3100

# ── Keystore Backend ───────────────────────────────────────────────────
# Options: "encrypted-file" or "env"
KEYSTORE_BACKEND=encrypted-file

# For encrypted-file backend:
KEYSTORE_PASSWORD=your-keystore-password
KEYSTORE_PATH=./agent-keystore.json

# For env backend (alternative):
# AGENT_PRIVATE_KEY=0x...

# ── 2FA Configuration (Optional) ───────────────────────────────────────
# Set TFA_ENABLED=true to require Telegram approval for signing operations
TFA_ENABLED=false
TFA_SERVER_URL=http://localhost:3200
TFA_SERVER_SECRET=your-secure-random-secret-here

# Which operations require 2FA (comma-separated)
# Options: sign-message, sign-transaction, sign-authorization
TFA_OPERATIONS=sign-message,sign-transaction,sign-authorization
