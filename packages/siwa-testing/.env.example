# Server
PORT=3000
SERVER_DOMAIN=localhost:3000
JWT_SECRET=test-secret-change-in-production

# Agent keystore
KEYSTORE_BACKEND=encrypted-file
KEYSTORE_PASSWORD=test-password-local-only
KEYSTORE_PATH=./agent-keystore.json

# Onchain (set for live mode — triggers real registration + onchain verification)
RPC_URL=https://sepolia.base.org
CHAIN_ID=84532
VERIFICATION_MODE=live

# Metadata storage (for live registration)
# STORAGE_MODE=base64          # "base64" (default) or "ipfs"
# PINATA_JWT=your-pinata-jwt   # Required if STORAGE_MODE=ipfs
# REGISTRATION_FILE=../assets/registration-template.json

# Mock registration defaults (used when --live is not set)
MOCK_AGENT_ID=1
MOCK_AGENT_REGISTRY=eip155:84532:0x8004A818BFB912233c491871b3d84c89A494BD9e
MOCK_CHAIN_ID=84532
