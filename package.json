{
  "name": "siwa-monorepo",
  "private": true,
  "packageManager": "pnpm@10.28.0",
  "type": "module",
  "scripts": {
    "build": "pnpm --filter siwa build",
    "clean": "pnpm --filter siwa run clean"
  },
  "devDependencies": {
    "typescript": "^5.5.0"
  }
}
