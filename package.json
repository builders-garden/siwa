{
  "name": "siwa-monorepo",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "pnpm --filter siwa build",
    "clean": "pnpm --filter siwa run clean"
  },
  "devDependencies": {
    "typescript": "^5.5.0"
  }
}
